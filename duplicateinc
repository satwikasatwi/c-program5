#include <stdio.h>

int main() {
    int i,j,cnt=0;
int arr[]={2,3,4,5,6,6};
int len=sizeof(arr)/sizeof(int);

for(i=0;i<len;i++)
{
    for(j=i+1;j<len;j++)
    {
        if(arr[i]==arr[j])
        cnt=cnt+1;
    }
}
    if(cnt==0)
    printf("unique");
    else
    printf("duplicate");
    

    return 0;
}
